# Non-secret config only. Real secrets live in the OS keychain (keytar).
# Copy to .env for local dev overrides.

# Override the SQLite DB location (default: app.getPath('userData')/inventory.db)
# TSC_DB_PATH=./dev.db

# Set to "1" to log SQL statements to the console
# TSC_DB_TRACE=0
